import type { HSLA, RGB, RGBA, colorDetails } from "../types";
export declare const RGBToHEX: ({ r, g, b, a }: RGBA) => string;
export declare const HSLToRGB: ({ h, s, l }: colorDetails) => RGB;
export declare const RGBToHSL: ({ r, g, b, a }: RGBA) => HSLA;
